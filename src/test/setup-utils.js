import { beforeAll, afterAll, afterEach } from 'vitest'

// Utility test setup - no MSW needed for utility functions
const server = null

beforeAll(() => {
  // No server setup needed for utility tests
})

afterEach(() => {
  // No cleanup needed for utility tests
})

afterAll(() => {
  // No server teardown needed for utility tests
})